# =============================================================================
# BitBasel Backend - Environment Variables Configuration
# =============================================================================
# This file shows all environment variables needed for the application
# For Railway deployment, set these in the Railway dashboard
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Railway will automatically provide DATABASE_URL when you add PostgreSQL
# Format: postgresql+asyncpg://user:password@host:port/database
DATABASE_URL=postgresql+asyncpg://user@localhost:5432/artbasel_db

# -----------------------------------------------------------------------------
# Security & JWT Configuration
# -----------------------------------------------------------------------------
# Generate a strong random secret key for production:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# QR code generation salt (generate similar to SECRET_KEY)
QR_SECRET_SALT=your-qr-secret-salt-change-in-production

# -----------------------------------------------------------------------------
# File Upload Configuration
# -----------------------------------------------------------------------------
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760  # 10MB in bytes

# -----------------------------------------------------------------------------
# Apple Sign-In Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get these values from your Apple Developer account
APPLE_TEAM_ID=your_team_id
APPLE_KEY_ID=your_key_id
APPLE_CLIENT_ID=com.yourapp.artbasel
APPLE_REDIRECT_URI=https://yourapp.com/auth/callback

# Apple Private Key (Railway Only - Base64 encoded)
# To generate: cat keys/5Y3L2S8R8R.p8 | base64
# Then set this in Railway dashboard as: APPLE_PRIVATE_KEY_BASE64
# APPLE_PRIVATE_KEY_BASE64=<base64-encoded-private-key>

# -----------------------------------------------------------------------------
# Geofence Configuration (Miami Art Basel)
# -----------------------------------------------------------------------------
BASEL_LAT=25.7907
BASEL_LON=-80.1300
BASEL_RADIUS_KM=5

# -----------------------------------------------------------------------------
# External Services (Optional)
# -----------------------------------------------------------------------------
# Google Maps API for geocoding (optional enhancement)
# GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# C++ Media Server URL (for livestreaming)
# Set to Railway internal URL if deploying media server separately
# Format: http://media-server.railway.internal:9001
# MEDIA_SERVER_URL=http://localhost:8080

# AI/LLM API Key (if using experimental features)
# GROQ=your-groq-api-key

# -----------------------------------------------------------------------------
# Railway-Specific Variables (Auto-provided)
# -----------------------------------------------------------------------------
# These are automatically set by Railway - DO NOT set manually:
# - PORT: The port your app should listen on
# - RAILWAY_ENVIRONMENT: current environment (production/staging)
# - DATABASE_URL: PostgreSQL connection string (if using Railway DB)
